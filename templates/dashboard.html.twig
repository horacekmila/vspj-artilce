{% extends "base.html.twig" %}

{% block body %}
    <div class="continer-fluid">
        <div class="col-12">
            <div class="row">
                <div class="col-2"><h3>Název</h3></div>
                <div class="col-3"><h3>Autor</h3></div>
            </div>
        </div>
        {% for article in articles %}
            {% if article.isActive %}
            <div class="col-12 space">
                <div class="row">
                <div class="col-2">{{ article.title }}</div>
                <div class="col-2">{{ article.getCreatedBy.firstname }}</div>
                <div class="col-1"> <a href={{ path("article_view", {"article": article.id}) }}><button class="btn btn-success col-12" type="button">Zobrazit</button></a></div>
                </div>
            </div>
            {% endif %}
        {% endfor %}
    </div>
{% endblock %}